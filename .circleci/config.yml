version: 2.1
orbs:
  newman: postman/newman@1.4.0

jobs:
  build:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - newman/run:
          collection: API Automation_Exercise.postman_collection.json
