version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run:
          name: Install Newman
          command: npm install -g newman
      - run:
          name: Run API tests
          command: newman run "API Automation_Exercise.postman_collection.json"
